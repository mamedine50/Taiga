# React Native / Expo exigent un node_modules « à plat » (comme npm).
# node-linker=hoisted évite les problèmes de résolution de Metro dans le monorepo.
node-linker=hoisted
# Autorise les scripts de build des dépendances natives (Expo, etc.).
enable-pre-post-scripts=true
